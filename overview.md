# AI 穿搭导购 Agent 体系搭建指引

## 本次结论

- 当前应用应从“预设规则的对话式应用”升级为“模型编排 + 受控工具 + 数据管道 + 评测监控”的 Agent 体系。
- 模型只负责理解需求、管理追问和生成解释；商品价格、库存、尺码、预算与禁忌品类等事实必须由工具查询与规则校验保证。

## 推荐架构

1. 用户层：对话、推荐卡、反馈和商品管理后台。
2. Agent 编排层：意图解析、会话状态、最多三轮追问、工具选择、输出校验。
3. 工具与规则层：商品检索、搭配组合、预算计算、尺码建议、库存与规则校验。
4. 数据层：商品与 SKU、会话与偏好、推荐记录、反馈、评测集、同步运行记录。
5. 运行保障：定时同步、幂等任务、失败重试、死信队列、链路追踪、离线评测、灰度发布和回滚。

## 实施顺序

- 先固化可验收的业务规则、工具契约和评测集。
- 再完成商品库、会话状态、推荐日志和管理后台。
- 接入 Agent 编排与结构化工具调用。
- 添加自动数据同步与运营监控。
- 使用回放评测、工具单测、端到端场景测试和灰度发布完成上线。

## 已完成的初始商品更新源

- 已生成 `AI穿搭导购_商品更新源.xlsx`，作为第一版 Excel 商品更新源。
- 商品主数据包含 70 条模拟商品：上装 18 条、裤装 14 条、半身裙 10 条、连衣裙 10 条、鞋履 10 条、配饰 8 条。
- 工作簿同时提供更新说明、字段字典、价格/库存/状态字段、可推荐计算列及状态高亮，可直接作为后续商品检索与预算校验的输入。

## 已完成的 Agent 后端接入

- 已在 `agent-backend/app/main.py` 建立本地 FastAPI 服务，启动时从 Excel 导入商品，并提供 `/api/catalog/import-xlsx` 手动刷新接口。
- 已实现 `/api/chat`：维护会话状态、最多三轮追问、连续处理“更显瘦 / 再便宜一点 / 不要裙子”，并以商品库存、上架状态、尺码、预算和禁忌品类作为硬校验。
- 已实现 `/health`、`/ready` 与 `/api/traces/{trace_id}`，使用 SQLite 保存商品数据、会话状态和每次推荐的可回放 trace。
- 已验证：Excel 刷新成功导入 70 条商品；完整搭配推荐与连续调整链路可用。

## 已发布的全栈 Agent 版本

- 秒哒 v12「衣橱顾问」版本已成功发布至：https://app-dyzbannedjwh.appmiaoda.com
- 已迁移为内置 Supabase 全栈架构：商品、会话偏好、对话 trace、导入日志四类数据表；chat、catalogRefresh、health、traceQuery 四个后端动作。
- 前端聊天、快捷标签和推荐卡均已改为调用后端，不再依赖前端硬编码推荐逻辑；新增商品管理区，可修改价格、库存、上架状态并刷新目录。
- 内部自检通过：`通勤 / 简约 / 165cm / M码 / 显瘦 / ¥1000内` 可获得 3 套搭配；在同一会话输入“再便宜一点，不要裙子”后，仍可在保留既有条件的前提下生成 3 套裤装搭配。
- 消费者界面已升级为移动优先的暖金「衣橱顾问」体验：全屏对话、底部抽屉筛选、衣橱推荐卡组、语音输入、场景冲突与无匹配解释状态均已通过回归验证。
- v11 新增「我的档案」与「我的衣橱」：基于匿名设备 profile_id 保存可跳过的软偏好、支持档案保存/清空/删除，以及衣橱单品增删改和闲置标记；这些信息不会放宽库存、上架、预算、禁忌品类与尺码等硬校验。
- v12 新增「穿衣日历」：支持月视图、从衣橱选择单品记录每日穿搭、同日更新、编辑与删除，以及基于真实记录生成的月度统计；不读取定位或真实天气数据。

## 可恢复代码基线（2026-09-11）

- GitHub 私有仓库：`https://github.com/spongebobbc018-oss/ai-wardrobe-agent`，`main` 分支是恢复基线。
- 已将本地可运行版本代码化：FastAPI 同源网页和 REST API、对话推荐、场景冲突/无匹配、浏览器语音输入、匿名档案、我的衣橱、穿衣日历与月度统计。
- 已补齐 `database/migrations/0001_initial_schema.sql`、`Dockerfile`、`docker-compose.yml`、GitHub Actions 校验流程、架构说明与恢复手册。
- 本地已验证：70 条商品导入、推荐接口、档案保存、衣橱新增、日历同日更新与月度统计；本机未安装 Docker，因此容器镜像尚未在本机实际构建。
- SQLite 数据库、环境变量、Token 和真实用户数据均被 `.gitignore` 排除，不上传到仓库。

## 下一步建议

- 将 SQLite Repository 替换为托管 PostgreSQL，并启用数据库每日备份。
- 将 Excel 更新源迁移为定时导入任务，或替换为腾讯文档表格、外部商品 API。
- 完成独立云端部署、健康检查与错误告警，再逐步引入登录、收藏、长期偏好、真实商品图片和个性化排序。
